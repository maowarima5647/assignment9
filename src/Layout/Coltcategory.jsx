import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Coltcategory = () => {
    const data = useLoaderData();
   // console.log(data)
    return (
        <div>
            fhdfhgdfhdfhg
        </div>
    );
};

export default Coltcategory;